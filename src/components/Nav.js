import React from 'react';
// import PT from 'prop-types';

class Nav extends React.Component {
    render () {
        return (
            <div>
                Nav
            </div>
        );
    }

    static propTypes = {
    
    }
}

export default Nav;